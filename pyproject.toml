[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kaneru-common"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["common*"]
